import React from "react";
import { connect } from "react-redux";

const MainVideo = () => {
  // const videoSrc = `https://www.youtube.com/embed/${video.id.videoId}`;

  return (
    <div>
      <iframe title="" src=""></iframe>
    </div>
  );
};

export default connect()(MainVideo);
